{{>layout/header}}

<h1>나의 정보</h1>

<div class="col-md-12">
    <div class="col-md-4">
        <form>
            <div class="form-group">
                <label for="username">아이디</label>
                <div>{{userD.username}}</div>
            </div>
            <div class="form-group">
                <label for="name">이름</label>
                <div>{{userD.name}}</div>
            </div>
            <div class="form-group">
                <label for="email"> 이메일 </label>
                <div>{{userD.email}}</div>
            </div>
        </form>

        <a href="/mypage/update/{{userD.username}}">수정하기</a>
    </div>
</div>

{{>layout/footer}}