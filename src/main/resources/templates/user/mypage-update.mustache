{{>layout/header}}

<h1>나의정보 수정</h1>

<div class="col-md-12">
    <div class="col-md-4">
        <form>
            <div class="form-group">
                <label for="title">아이디</label>
                <div><input type="text" class="form-control" id="author" value="{{users.username}}" readonly></div>
            </div>
            <div class="form-group">
                <label for="title">비밀번호</label>
                <div><input type="text" class="form-control" id="author" value="password"></div>
            </div>
            <div class="form-group">
                <label for="title">이름</label>
                <div><input type="text" class="form-control" id="author" value="{{users.name}}"></div>
            </div>
            <div class="form-group">
                <label for="author"> 이메일 </label>
                <div><input type="text" class="form-control" id="author" value="{{users.email}}"></div>
            </div>
        </form>
        <button type="button" class="btn btn-primary" id="btn-userUpdate">수정</button>
    </div>
</div>

{{>layout/footer}}